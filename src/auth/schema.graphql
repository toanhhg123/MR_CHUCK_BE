input AuthRequest {
  email: String!
  password: String!
}

type AuthResponse {
  accessToken: String!
  refreshTokens: String!
}

type Query {
  getMe: User
}

type Mutation {
  login(authRequest: AuthRequest!): AuthResponse!
}
